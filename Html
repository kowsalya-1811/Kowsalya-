<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ammu's Butterfly Portfolio</title>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Dancing Script', cursive;
            background: linear-gradient(135deg, #f5e3e6 0%, #d9e4f5 100%);
            color: #6b5b95;
            overflow-x: hidden;
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        header {
            text-align: center;
            padding: 40px 0;
            position: relative;
            overflow: hidden;
        }

        h1 {
            font-size: 5rem;
            color: #ff6b6b;
            text-shadow: 3px 3px 0px rgba(0, 0, 0, 0.2);
            margin-bottom: 10px;
            animation: pulse 2s infinite;
        }

        h2 {
            font-size: 2.5rem;
            color: #6b5b95;
            margin-bottom: 30px;
            text-shadow: 2px 2px 0px rgba(255, 255, 255, 0.5);
        }

        .tagline {
            font-size: 1.8rem;
            color: #ff6b6b;
            margin-bottom: 30px;
        }

        .butterfly {
            position: absolute;
            width: 40px;
            height: 40px;
            background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
            border-radius: 50% 50% 50% 50% / 60% 60% 40% 40%;
            box-shadow: 0 0 20px rgba(255, 107, 107, 0.5);
            animation: fly 15s infinite ease-in-out;
            z-index: -1;
        }

        .butterfly::before,
        .butterfly::after {
            content: '';
            position: absolute;
            background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
            border-radius: 50%;
        }

        .butterfly::before {
            width: 30px;
            height: 15px;
            top: -7px;
            left: 5px;
            transform: rotate(-45deg);
            box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
        }

        .butterfly::after {
            width: 30px;
            height: 15px;
            top: -7px;
            right: 5px;
            transform: rotate(45deg);
            box-shadow: -2px 2px 5px rgba(0, 0, 0, 0.2);
        }

        .section {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 20px;
            padding: 30px;
            margin-bottom: 40px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            transform: translateY(50px);
            opacity: 0;
            transition: transform 0.8s ease, opacity 0.8s ease;
        }

        .section.visible {
            transform: translateY(0);
            opacity: 1;
        }

        .section-title {
            font-size: 2.8rem;
            color: #ff6b6b;
            text-align: center;
            margin-bottom: 25px;
            text-shadow: 2px 2px 0px rgba(0, 0, 0, 0.1);
        }

        .about-content {
            display: flex;
            align-items: center;
            gap: 30px;
        }

        .profile-img {
            width: 200px;
            height: 200px;
            border-radius: 50%;
            object-fit: cover;
            border: 5px solid #ff6b6b;
            box-shadow: 0 0 20px rgba(255, 107, 107, 0.5);
            animation: float 6s ease-in-out infinite;
        }

        .about-text {
            flex: 1;
            font-size: 1.5rem;
        }

        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
        }

        .skill {
            background: linear-gradient(135deg, #f5e3e6 0%, #d9e4f5 100%);
            padding: 20px;
            border-radius: 15px;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        .skill:hover {
            transform: translateY(-10px);
        }

        .skill i {
            font-size: 2.5rem;
            color: #ff6b6b;
            margin-bottom: 15px;
        }

        .skill h3 {
            font-size: 1.8rem;
            margin-bottom: 10px;
        }

        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 25px;
        }

        .project {
            background: linear-gradient(135deg, #f5e3e6 0%, #d9e4f5 100%);
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        .project:hover {
            transform: scale(1.05);
        }

        .project-img {
            width: 100%;
            height: 200px;
            background: #6b5b95;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5rem;
        }

        .project-content {
            padding: 20px;
        }

        .project h3 {
            font-size: 1.8rem;
            margin-bottom: 10px;
            color: #ff6b6b;
        }

        .contact-content {
            text-align: center;
            font-size: 1.5rem;
        }

        .social-links {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }

        .social-links a {
            display: inline-block;
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #6b5b95;
            font-size: 1.8rem;
            text-decoration: none;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        .social-links a:hover {
            transform: translateY(-5px);
        }

        footer {
            text-align: center;
            padding: 30px 0;
            font-size: 1.5rem;
            color: #6b5b95;
        }

        /* Animations */
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        @keyframes fly {
            0% { transform: translate(0, 0) rotate(0deg); }
            25% { transform: translate(200px, 300px) rotate(10deg); }
            50% { transform: translate(400px, 100px) rotate(-10deg); }
            75% { transform: translate(600px, 300px) rotate(5deg); }
            100% { transform: translate(1000px, 0) rotate(0deg); }
        }

        @keyframes float {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
            100% { transform: translateY(0px); }
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            h1 {
                font-size: 3.5rem;
            }
            
            h2 {
                font-size: 2rem;
            }
            
            .about-content {
                flex-direction: column;
                text-align: center;
            }
            
            .section-title {
                font-size: 2.2rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Ammu</h1>
            <h2>Butterfly-Themed Portfolio</h2>
            <p class="tagline">Transforming Ideas into Beautiful Reality</p>
            
            <!-- Animated Butterflies -->
            <div class="butterfly" style="top: 20%; left: 10%; animation-delay: 0s;"></div>
            <div class="butterfly" style="top: 60%; left: 5%; animation-delay: 2s;"></div>
            <div class="butterfly" style="top: 30%; left: 15%; animation-delay: 4s;"></div>
            <div class="butterfly" style="top: 70%; left: 20%; animation-delay: 1s;"></div>
            <div class="butterfly" style="top: 40%; right: 10%; animation-delay: 3s;"></div>
            <div class="butterfly" style="top: 80%; right: 5%; animation-delay: 5s;"></div>
        </header>
        
        <section class="section" id="about">
            <h2 class="section-title">About Me</h2>
            <div class="about-content">
                <div class="profile-img-container">
                    <div class="profile-img" style="background-color: #ff6b6b; display: flex; align-items: center; justify-content: center; color: white; font-size: 5rem;">
                        <i class="fas fa-user"></i>
                    </div>
                </div>
                <div class="about-text">
                    <p>Hello! I'm Ammu, a creative developer with a passion for building beautiful, interactive web experiences. Like a butterfly, I believe in transformation and growth, constantly evolving my skills and embracing new challenges.</p>
                    <p>My journey in design and development started 3 years ago, and I've been spreading my wings ever since!</p>
                </div>
            </div>
        </section>
        
        <section class="section" id="skills">
            <h2 class="section-title">My Skills</h2>
            <div class="skills-grid">
                <div class="skill">
                    <i class="fas fa-paint-brush"></i>
                    <h3>Web Design</h3>
                    <p>Creating beautiful, user-friendly interfaces with a focus on aesthetics and usability.</p>
                </div>
                <div class="skill">
                    <i class="fas fa-code"></i>
                    <h3>Frontend Development</h3>
                    <p>Building responsive and interactive websites using HTML, CSS, and JavaScript.</p>
                </div>
                <div class="skill">
                    <i class="fas fa-mobile-alt"></i>
                    <h3>Responsive Design</h3>
                    <p>Ensuring websites look great on all devices, from desktops to smartphones.</p>
                </div>
                <div class="skill">
                    <i class="fas fa-magic"></i>
                    <h3>UI/UX Design</h3>
                    <p>Designing intuitive user experiences with a focus on simplicity and functionality.</p>
                </div>
            </div>
        </section>
        
        <section class="section" id="projects">
            <h2 class="section-title">My Projects</h2>
            <div class="projects-grid">
                <div class="project">
                    <div class="project-img">Butterfly Effect</div>
                    <div class="project-content">
                        <h3>Butterfly Effect</h3>
                        <p>An interactive website showcasing the beauty of butterflies with CSS animations.</p>
                    </div>
                </div>
                <div class="project">
                    <div class="project-img">Floral Design</div>
                    <div class="project-content">
                        <h3>Floral Design Studio</h3>
                        <p>An e-commerce website for a floral design studio with animated elements.</p>
                    </div>
                </div>
                <div class="project">
                    <div class="project-img">Nature Blog</div>
                    <div class="project-content">
                        <h3>Nature Blog</h3>
                        <p>A blog dedicated to nature photography and environmental conservation.</p>
                    </div>
                </div>
            </div>
        </section>
        
        <section class="section" id="contact">
            <h2 class="section-title">Contact Me</h2>
            <div class="contact-content">
                <p>Feel free to reach out if you'd like to work together or just want to say hello!</p>
                <div class="social-links">
                    <a href="#"><i class="fab fa-instagram"></i></a>
                    <a href="#"><i class="fab fa-twitter"></i></a>
                    <a href="#"><i class="fab fa-linkedin-in"></i></a>
                    <a href="#"><i class="fab fa-github"></i></a>
                </div>
            </div>
        </section>
        
        <footer>
            <p>© 2023 Ammu's Butterfly Portfolio. All rights reserved.</p>
        </footer>
    </div>

    <script>
        // Function to check if an element is in the viewport
        function isInViewport(element) {
            const rect = element.getBoundingClientRect();
            return (
                rect.top >= 0 &&
                rect.left >= 0 &&
                rect.bottom <= (window.innerHeight || document.documentElement.clientHeight) &&
                rect.right <= (window.innerWidth || document.documentElement.clientWidth)
            );
        }

        // Function to add the 'visible' class when section is in viewport
        function checkVisibility() {
            const sections = document.querySelectorAll('.section');
            sections.forEach(section => {
                if (isInViewport(section)) {
                    section.classList.add('visible');
                }
            });
        }

        // Check visibility on load and scroll
        window.addEventListener('load', checkVisibility);
        window.addEventListener('scroll', checkVisibility);

        // Create more butterflies dynamically
        function createButterflies() {
            const header = document.querySelector('header');
            for (let i = 0; i < 8; i++) {
                const butterfly = document.createElement('div');
                butterfly.classList.add('butterfly');
                butterfly.style.top = Math.random() * 80 + '%';
                butterfly.style.left = Math.random() * 90 + '%';
                butterfly.style.animationDelay = Math.random() * 5 + 's';
                header.appendChild(butterfly);
            }
        }

        createButterflies();
    </script>
</body>
</html>
